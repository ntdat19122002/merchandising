<script>
    if(!localStorage.getItem('uuid')){
        localStorage.setItem('uuid',generateUUID())
    }

    function generateUUID() {
        return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {
            const r = Math.random() * 16 | 0; // Generate a random number
            const v = c === 'x' ? r : (r & 0x3 | 0x8); // Set the variant and version bits
            return v.toString(16); // Convert to hexadecimal
        });
    }
</script>

{% schema %}
{
  "name": "Merchandising",
  "target": "body",
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "product",
      "autofill": true
    },
    {
      "type": "color",
      "id": "colour",
      "label": "Star Colour",
      "default": "#ff0000"
    }
  ]
}
{% endschema %}

